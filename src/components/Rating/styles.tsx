import styled from "styled-components";

const StyledRating = styled.div`
  display: flex;
  gap: 10px;
  align-items: center;
  justify-content: center;
`;

const StyledRatingText = styled.span`
  font-size: 16px;
  font-weight: 500;
  margin-left: 5px;
  color: #ffffff;
`;

export { StyledRating, StyledRatingText };
